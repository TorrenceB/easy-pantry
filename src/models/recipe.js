const Recipe = ({
  id,
  title,
  image,
  instructions,
  summary,
  readyInMinutes,
  healthScore,
  servings,
  aggregateLikes,
  diets,
}) => ({
  id,
  title,
  image,
  instructions,
  summary,
  readyInMinutes,
  healthScore,
  servings,
  aggregateLikes,
  diets,
});

export default Recipe;
